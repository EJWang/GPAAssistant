package view;



import controller.Controller;

public class NewUserFrame extends MainFrame {
  
  /**
   *  Use inheritance to construct NewUserFrame object based on MainFrame
   * 
   * @param controller Default controller used for mainFrame
   */
  public NewUserFrame(Controller controller) {
    super(controller);

  }

}
